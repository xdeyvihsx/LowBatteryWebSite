import { HeroBackground } from "@/sections/HeroSection/components/HeroBackground";

export const HeroSection = () => {
  return (
    <section className="relative bg-neutral-900 box-border caret-transparent w-full">
      <HeroBackground />
    </section>
  );
};
